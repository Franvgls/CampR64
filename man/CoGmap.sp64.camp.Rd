% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CoGmap.sp64.camp.R
\name{CoGmap.sp64.camp}
\alias{CoGmap.sp64.camp}
\title{Centro de gravedad geográfico de una especie sobre el mapa, con deriva temporal}
\usage{
CoGmap.sp64.camp(
  gr,
  esp,
  camps,
  zona = "porc",
  dns = c("local", "serv"),
  cor.time = TRUE,
  incl2 = FALSE,
  traj = TRUE,
  grad = TRUE,
  label = c("ends", "all", "none"),
  ti = TRUE,
  sub = TRUE,
  idi = "l",
  pal = "viridis",
  col.traj = "grey60",
  cex = 1.3,
  map.args = list()
)
}
\arguments{
\item{gr}{Grupo de la especie: 1 peces, 2 crustáceos, 3 moluscos, 4 equinodermos, 5 invertebrados}

\item{esp}{Código numérico o carácter con tres espacios}

\item{camps}{Una campaña o vector de campañas}

\item{zona}{"cant", "porc", "arsa", "medi"}

\item{dns}{"local" o "serv"}

\item{cor.time}{Si TRUE corrige por duración del lance}

\item{incl2}{Si TRUE incluye lances especiales}

\item{traj}{Si TRUE (defecto) une los centroides en orden cronológico}

\item{grad}{Si TRUE (defecto) colorea los puntos con un gradiente temporal}

\item{label}{Etiquetas de año: "ends" (defecto) sólo primero y último,
"all" todos (usa plotrix::thigmophobe si está disponible), "none" ninguno}

\item{ti}{Título. TRUE (defecto) = nombre científico; carácter = literal; FALSE = sin título}

\item{sub}{Subtítulo. TRUE (defecto) = rango de años; carácter = literal; FALSE = sin subtítulo}

\item{idi}{Idioma del nombre de especie: "l" latín, "e" español, "i" inglés}

\item{pal}{Paleta para el gradiente temporal (ver \code{hcl.colors})}

\item{col.traj}{Color de la línea de trayectoria}

\item{cex}{Escala de los puntos del centroide}

\item{map.args}{Lista de argumentos para \code{maparea64()} (por defecto vacía:
llamada sin argumentos). Úsalo si necesitas pasarle parámetros de área.}
}
\value{
(invisible) data.frame con \code{camp, year, minlat, cog.lat, maxlat,
  minlng, cog.lng, maxlng}
}
\description{
Combina el centro de gravedad longitudinal y latitudinal (medias ponderadas
por abundancia) de la especie para cada campaña y proyecta el centroide
resultante sobre el mapa del área, conectando los puntos en orden cronológico
para mostrar la deriva del centro de distribución a lo largo de la serie.
Pensado para campañas de geometría no lineal (p. ej. Porcupine), donde
latitud y longitud son ejes informativos e independientes.
}
\examples{
\dontrun{
  CoGmap.sp64.camp(1, 50, Psh, "porc")
  d <- CoGmap.sp64.camp(1, 50, Psh, "porc", label = "all")
}
}
\seealso{
Other Distribuciones geográficas: 
\code{\link{CoGLat.sp64.camp}()}
}
\concept{Distribuciones geográficas}
