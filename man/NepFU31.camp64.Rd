% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NepFU31.camp64.R
\name{NepFU31.camp64}
\alias{NepFU31.camp64}
\title{Información anual sobre cigala en la FU31 en el Cantábrico}
\usage{
NepFU31.camp64(
  camp,
  zona = "cant",
  dns = c("local", "serv"),
  plot = TRUE,
  es = FALSE,
  ti = TRUE,
  ICESlab = FALSE,
  ceros = T,
  ICESrectcol = 1,
  ICESrect = TRUE,
  FU = "FU31",
  ColFU = "white",
  dens = 20,
  places = TRUE,
  out.dat = TRUE,
  bw = FALSE,
  escmult = 0.25,
  leg = TRUE,
  cexleg = 0.9,
  cor.time = TRUE
)
}
\arguments{
\item{camp}{Campaña de la que se extraen los datos: año comcreto (XX): Demersales "NXX"}

\item{dns}{Sólo disponible para el Cantábrico "Cant", combinados con "dnsred" busca los datos en el servidor de Santander si se han creado las odbcs}

\item{plot}{Saca el gráfico (T) o lo omite para dejar sólo los datos (F)}

\item{es}{si T letreros en español, si F en inglés (F por defecto)}

\item{ICESlab}{Si T incluye las etiquetas de los rectángulos ICES}

\item{ceros}{si T saca los puntos sin captura de cigala como puntos negros, si F no se representan los ceros}

\item{ICESrect}{Si T saca los rectangulos ices de 1 grado de latitud por medio de longitud}

\item{FU}{pinta la FU31}

\item{ColFU}{color de relleno de la FU, blanco por defecto, con dens=0 se omite el relleno.}

\item{dens}{valores de 20 hace que el color permita ver el fondo con los ICES rects y la batimetría, más lo deja en color sólido, 0 quita el relleno del todo.}

\item{places}{Si T saca etiquetas de principales ciudades en el mapa, si F se omiten los letreros}

\item{out.dat}{Si T el resultado final de la función es la figura en pantalla, pero los datos de abundancia de cigala en cada FU con datos de biomasa, numero, desviaciones estándar y número de lances en cada FU.}

\item{year}{si T incluye una columna con el año al final de los datos}

\item{plotnep}{si T presenta todos los lances en los que ha habido cigala en la campaña, además de los lances en cada FU, con o sín captura. Si F sólo saca los lances en cada FU sin marcar si ha habido cigala o no.}

\item{icesrectcol}{Color para los rectángulos ICES}

\item{ICESlabcex}{tamaño del ICESlab en cex, .5 por defecto subirlo si se quiere más grande}
}
\value{
Produce un gráfico con los lances en los que ha habido cigala en el lance y especialmente los lances en cada FU dentro de Demersales FU25,FU26 y FU31
}
\description{
Función de acceso a datos:
Extrae las características de los lances para una campaña determinada y las capturas de cigala por unidad funcional
}
\details{
Un problema que ocurre al utilizar el CampR con ficheros dbf de las primeras campañas
puede ser que al fichero lanceXXX.dbf le falte algún campo, habitualmente
el campo \strong{ESTN} utilizado en las últimas versiones del \strong{CAMP} para ligar lances con las estaciones de CTD.
El error usual es \strong{$ operator is invalid for atomic vectors}
Si se detecta este error revisar la estructura de lanceXXX.dbf con la de
otros ficheros de lances de los últimos años
}
\examples{
  NepFU31.camp("N21")
}
\seealso{
Other mapas, NEP: 
\code{\link{NepFU25.camp64}()},
\code{\link{NepFU26.camp64}()},
\code{\link{NepFU30.camp64}()}
}
\concept{mapas, NEP}
