% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logabage.camp64.r
\name{logabage.camp64}
\alias{logabage.camp64}
\title{Descenso logarítmico de abundancia por cohorte}
\usage{
logabage.camp64(
  gr,
  esp,
  camps,
  zona = "cant",
  dns = c("local", "serv"),
  plus = 8,
  cor.time = TRUE,
  clms = 2,
  layout = NA
)
}
\arguments{
\item{gr}{Grupo taxonómico (solo peces — requiere ALK).}

\item{esp}{Código numérico de especie. Solo admite una especie.}

\item{camps}{Vector de campañas de la serie histórica (p.ej. \code{Nsh}).}

\item{zona}{Zona: \code{"cant"}, \code{"porc"}, \code{"arsa"}, \code{"medi"}.}

\item{dns}{Origen de datos: \code{"local"} o \code{"serv"}.}

\item{plus}{Edad plus — edades superiores acumuladas. Por defecto \code{8}.}

\item{cor.time}{Si \code{TRUE} corrige por duración del lance.}

\item{clms}{Número de columnas del panel lattice.}

\item{layout}{Vector \code{c(filas, columnas)} del panel lattice.
Si \code{NA} (por defecto) se calcula automáticamente.}
}
\value{
Invisiblemente, imprime la matriz edad × campaña. El gráfico
lattice se produce como efecto lateral.
}
\description{
Genera un panel lattice (un subgráfico por cohorte) con el descenso
de log(abundancia) a lo largo de la edad, permitiendo estimar la
mortalidad total (Z) por cohorte como la pendiente de la regresión.
}
\details{
Cada panel muestra una cohorte (año de nacimiento = año campaña − edad).
La línea roja discontinua es la regresión de toda la serie completa,
como referencia. El valor en la esquina superior derecha de cada panel
es la pendiente de la regresión de esa cohorte (estimador de −Z).

La campaña \code{"N87"} se trata como \code{NA} por ausencia de ALK ese año.
El layout se calcula automáticamente si no se especifica.
}
\examples{
\dontrun{
# Jurel (esp=90) — Cantábrico
logabage.camp64(gr=1, esp=90, camps=Nsh, zona="cant", dns="local")

# Caballa (esp=74) — Cantábrico
logabage.camp64(gr=1, esp=74, camps=Nsh, zona="cant", dns="local")

# Gallo whiff (esp=43) — Porcupine
logabage.camp64(gr=1, esp=43, camps=Psh, zona="porc", dns="local")
}#' @export
 
}
\seealso{
\code{\link[=bubbage.camp64]{bubbage.camp64()}} para burbujas de abundancia,
\code{\link[=edadstr.camp64]{edadstr.camp64()}} para abundancias a la edad por estrato

Other edades: 
\code{\link{AbAgStatRec.camp64}()},
\code{\link{GetAlk.camp64}()},
\code{\link{bubbage.camp64}()},
\code{\link{datagegr.camp64}()},
\code{\link{edadsect.camp64}()},
\code{\link{edadstr.camp64}()},
\code{\link{grafedtal.camp64}()},
\code{\link{grafedtal.camps64}()},
\code{\link{maphistage64}()}
}
\concept{edades}
